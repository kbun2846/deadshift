{
  "name": "deadshift",
  "version": "0.9.85",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": { "peerjs": "1.5.5", "three": "0.180.0" },
  "devDependencies": { "vite": "7.1.7" }
}
